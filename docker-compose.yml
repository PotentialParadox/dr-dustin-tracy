services: 
  web: 
    build: .
    ports: 
      - "80:80" 
      - "443:443"
    environment: 
      - VERSION="1.0.0" 
    volumes:
      - /etc/nginx/conf.d/:/etc/nginx/conf.d/:ro
      - /var/www/certbot/:/var/www/certbot/:ro
  certbot:
    image: certbot/certbot:latest
    volumes:
      - /var/www/certbot/:/var/www/certbot/:rw
      - /var/www/certbot/conf/:/etc/nginx/ssl/:rw
    depends_on:
      - web
